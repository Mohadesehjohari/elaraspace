import assert from 'node:assert/strict';
import {mkdirSync,writeFileSync} from 'node:fs';
import {chromium} from 'playwright';
const out='browser-artifacts';mkdirSync(out,{recursive:true});
const browser=await chromium.launch({headless:true});
const mobile=['exercise','language','tasks','home','ranking','social','books','freedom'];
const desktop=['home','tasks','language','books','exercise','ranking','social','freedom'];
const used=['hero-landscape.webp','missions-rocket.webp','streak-flame.webp','friends-tab.webp','friends-group-icon.webp','nav-home-default.webp','nav-home-active.webp','nav-tasks-default.webp','nav-tasks-active.webp','nav-language-default.webp','nav-language-active.webp','nav-library-default.webp','nav-library-active.webp','nav-ranking-default.webp','nav-ranking-active.webp','nav-exercise-default.webp','nav-exercise-active.webp','icon-mode-night-default.webp','icon-mode-night-active.webp','icon-notifications-read.webp','icon-notifications-unread.webp','icon-exercise-dumbbell.webp','icon-wellness-heartbeat.webp','icon-ranking-trophy.webp','icon-tasks-check-alpha.webp','brand-elara-app-mark.webp'];
const sizes=[[320,659],[375,659],[390,659],[430,659],[1440,1000],[1648,928],[1920,1000]];
const results=[],failures=[];
// Visual audit sheet: lets the acceptance artifact prove the real content of ambiguous/new assets.
{
 const assetPage=await browser.newPage({viewport:{width:1100,height:760},deviceScaleFactor:1});
 await assetPage.setContent(`<base href="http://127.0.0.1:4173/"><style>body{margin:0;background:#08162b;color:white;font:16px sans-serif;display:grid;grid-template-columns:repeat(3,1fr);gap:12px;padding:16px}.cell{height:330px;border:1px solid #40527d;border-radius:14px;display:grid;grid-template-rows:1fr auto;place-items:center;background:#0d1b32;overflow:hidden}.cell img{max-width:280px;max-height:270px;object-fit:contain}.cell b{padding:8px;font-size:13px}</style>
 <div class="cell"><img src="assets/ui/d0c0ad2f-d54b-5634-a886-1bf2de26718a.webp"><b>UUID</b></div>
 <div class="cell"><img src="assets/ui/notification-button.webp"><b>notification-button</b></div>
 <div class="cell"><img src="assets/ui/icon-wellness-weight-scale.webp"><b>weight-scale</b></div>
 <div class="cell"><img src="assets/ui/friends-tab.webp"><b>friends-tab</b></div>
 <div class="cell"><img src="assets/ui/friends-group-icon.webp"><b>friends-group</b></div>
 <div class="cell"><img src="assets/ui/brand-elara-app-mark.webp"><b>named app mark</b></div>`,{waitUntil:'load'});
 await assetPage.waitForTimeout(200);await assetPage.screenshot({path:out+'/asset-review-sheet.png',fullPage:false});await assetPage.close();
}
function fixture(){if(localStorage.getItem('elara-test-seeded'))return;const d=new Date(),today=[d.getFullYear(),String(d.getMonth()+1).padStart(2,'0'),String(d.getDate()).padStart(2,'0')].join('-');localStorage.setItem('elara_space_v1',JSON.stringify({version:1,xp:0,tasks:[{id:'qa-task',text:'QA task',date:today,priority:'2',completed:false,xpAwarded:false,createdAt:Date.now()}],goals:[{id:'qa-goal',title:'QA goal',horizon:'short',steps:[{id:'qa-step',text:'QA step',done:false}]},{id:'qa-goal-2',title:'QA goal 2',horizon:'short',steps:[]},{id:'qa-goal-3',title:'QA goal 3',horizon:'short',steps:[]},{id:'qa-goal-4',title:'QA goal 4',horizon:'short',steps:[]},{id:'qa-goal-5',title:'QA goal 5',horizon:'short',steps:[]},{id:'qa-goal-6',title:'QA goal 6',horizon:'short',steps:[]}],habits:[{id:'qa-habit',title:'مطالعه',days:[],rewardDays:[]}]}));localStorage.setItem('elara_visual_wardrobe_guest',JSON.stringify({frame:'bronze'}));localStorage.setItem('elara-test-seeded','1')}
async function init(page,{seed=false}={}){
 await page.route('**/cloud.js',route=>route.abort());
 await page.addInitScript(()=>{window.__elaraPaint=[];const start=performance.now();let frames=0;const sample=()=>{const shell=document.querySelector('.shell'),nav=document.querySelector('.bottom-nav [data-elara-tab="home"] .elara-nav-art, .sidebar [data-elara-tab="home"] .elara-nav-art'),booting=document.documentElement.hasAttribute('data-elara-booting');window.__elaraPaint.push({ms:Math.round(performance.now()-start),booting,shell:shell?getComputedStyle(shell).visibility:'not-mounted',navPresent:!!nav,navReady:!!(nav?.complete&&nav?.naturalWidth),navSrc:nav?.getAttribute('src')||null});if(++frames<300&&(booting||frames<26))requestAnimationFrame(sample)};addEventListener('DOMContentLoaded',()=>requestAnimationFrame(sample),{once:true})});
 if(seed)await page.addInitScript(fixture);
 await page.goto('http://127.0.0.1:4173/#home',{waitUntil:'domcontentloaded',timeout:30000});
 await page.waitForFunction(()=>window.ElaraNavigation&&window.ElaraReferenceHome&&window.ElaraOpen&&!document.documentElement.hasAttribute('data-elara-booting'),null,{timeout:30000});
 await page.waitForFunction(()=>{const x=document.querySelector('.bottom-nav [data-elara-tab="home"] .elara-nav-art, .sidebar [data-elara-tab="home"] .elara-nav-art');return !!x&&x.complete&&x.naturalWidth>0},null,{timeout:15000});
 const first=await page.evaluate(()=>window.__elaraPaint||[]);assert.equal(first.some(x=>x.booting&&x.shell==='visible'),false,'legacy shell painted during boot');const released=first.find(x=>!x.booting);assert.ok(released,'no visible frame sampled');assert.equal(released.navPresent,true,'nav artwork installed after first visible shell frame');
 await page.addStyleTag({content:'#cloud-layer{display:none!important}body:not(.cloud-ready) .shell,body.cloud-locked .shell,body:not(.cloud-ready) .bottom-nav{visibility:visible!important}*,*:before,*:after{animation:none!important;transition:none!important;scroll-behavior:auto!important}'});
 await page.evaluate(()=>{document.body.classList.add('cloud-ready');document.body.classList.remove('cloud-locked');document.getElementById('cloud-layer')?.setAttribute('hidden','');window.ElaraNavigation.render();window.ElaraReferenceHome.render();window.ElaraOpen('home',{history:'replace'})});
 await page.waitForTimeout(350);
 if(seed){await page.evaluate(()=>localStorage.removeItem('elara-test-seeded'));await page.evaluate(fixture);await page.evaluate(()=>window.dispatchEvent(new CustomEvent('elara:hydrate',{detail:JSON.parse(localStorage.getItem('elara_space_v1'))})));await page.evaluate(()=>window.ElaraReferenceHome.render());await page.waitForTimeout(170)}
 return first;
}
const rect=(page,sel)=>page.locator(sel).first().evaluate(el=>{const r=el.getBoundingClientRect();return {x:Math.round(r.x),y:Math.round(r.y),w:Math.round(r.width),h:Math.round(r.height)}});
async function firstPaintReload(page,mode){if(mode==='hard'){const cdp=await page.context().newCDPSession(page);await cdp.send('Network.setCacheDisabled',{cacheDisabled:true});await page.reload({waitUntil:'domcontentloaded'});await page.waitForFunction(()=>!document.documentElement.hasAttribute('data-elara-booting'));await cdp.send('Network.setCacheDisabled',{cacheDisabled:false});await cdp.detach()}else{await page.reload({waitUntil:'domcontentloaded'});await page.waitForFunction(()=>!document.documentElement.hasAttribute('data-elara-booting'))}return page.evaluate(()=>({shell:getComputedStyle(document.querySelector('.shell')).visibility,navSrc:document.querySelector('.bottom-nav [data-elara-tab="home"] .elara-nav-art, .sidebar [data-elara-tab="home"] .elara-nav-art')?.getAttribute('src'),paint:window.__elaraPaint||[]}))}

async function bootTriptych(width,height){
 const page=await browser.newPage({viewport:{width,height},deviceScaleFactor:1,hasTouch:width<=700});
 await page.route('**/cloud.js',route=>route.abort());
 await page.route('**/assets/ui/*.webp',async route=>{await new Promise(r=>setTimeout(r,220));await route.continue()});
 await page.goto('http://127.0.0.1:4173/#home',{waitUntil:'domcontentloaded',timeout:30000});
 await page.waitForFunction(()=>document.documentElement.hasAttribute('data-elara-booting'),null,{timeout:3000});
 await page.waitForTimeout(70);
 const before=await page.evaluate(()=>{const visible=el=>{if(!el)return false;const s=getComputedStyle(el),r=el.getBoundingClientRect();return s.visibility!=='hidden'&&s.opacity!=='0'&&r.width>0&&r.height>0};return {shell:visible(document.querySelector('.shell')),bottom:visible(document.querySelector('.bottom-nav')),dock:visible(document.querySelector('.elara-desktop-dock'))}});
 assert.deepEqual(before,{shell:false,bottom:false,dock:false},'partial navigation leaked during boot');
 await page.screenshot({path:`${out}/boot-${width}-before-release.png`,fullPage:false});
 await page.waitForFunction(()=>!document.documentElement.hasAttribute('data-elara-booting'),null,{timeout:5000});
 const releaseState=await page.evaluate(()=>[...document.querySelectorAll('.bottom-nav .elara-nav-art,.sidebar .elara-nav-art')].filter(x=>getComputedStyle(x).display!=='none').every(x=>x.complete&&x.naturalWidth>0));
 assert.equal(releaseState,true,'nav artwork was not decoded at shell release');
 await page.screenshot({path:`${out}/boot-${width}-release.png`,fullPage:false});await page.waitForTimeout(500);await page.screenshot({path:`${out}/boot-${width}-plus500ms.png`,fullPage:false});
 await page.close();
}
await bootTriptych(390,659);await bootTriptych(1648,928);
for(const [width,height] of sizes){
 const page=await browser.newPage({viewport:{width,height},deviceScaleFactor:1,hasTouch:width<=700});const item={width,height,ui404:[],errors:[]};page.on('response',r=>{if(r.status()===404&&r.url().includes('/assets/ui/'))item.ui404.push(r.url())});page.on('pageerror',e=>item.errors.push(String(e)));
 try{
  item.paint=await init(page,{seed:width===390});const sel=width<=700?'.bottom-nav':'.sidebar .elara-sidebar-primary';
  item.routes=await page.locator(sel+' [data-elara-tab]').evaluateAll(nodes=>nodes.map(n=>n.dataset.elaraTab));assert.deepEqual(item.routes,width<=700?mobile:desktop);
  item.header=await rect(page,'.topbar');item.hero=await rect(page,'.ref-hero');item.streak=await rect(page,'#ref-streak-card');item.grid=await rect(page,'.ref-home-grid');
  item.overflow=await page.evaluate(()=>Math.max(0,document.documentElement.scrollWidth-innerWidth));item.scroll=await page.evaluate(()=>Math.max(0,Math.max(document.documentElement.scrollHeight,document.body.scrollHeight)-innerHeight));
  const hasTasks=await page.locator('#panel-home .ref-task-check').count()>0,hasHabits=await page.locator('#panel-home .ref-habit-row').count()>0;
  item.tasksHit=hasTasks?await rect(page,'#panel-home .ref-task-check'):null;item.habitHit=hasHabits?await rect(page,'#panel-home .ref-habit-row'):null;
  item.track=hasHabits?await page.locator('#panel-home .ref-habit-row .elara-track').first().evaluate(el=>Math.round(el.getBoundingClientRect().height)):null;
  assert.ok(item.overflow<=2,'horizontal overflow '+item.overflow);assert.ok(item.hero.h>=100&&item.streak.h>0&&item.grid.w>0,'Home geometry missing');
  assert.equal(await page.locator(`${sel} [data-elara-tab="home"] .elara-nav-art`).count(),1,'actual Home image missing');
  assert.equal(await page.locator(`${sel} [data-elara-tab="tasks"] .elara-nav-art`).count(),1,'Tasks artwork missing');
  assert.ok((await page.locator(`${sel} [data-elara-tab="tasks"] .elara-nav-art`).getAttribute('src'))?.includes('nav-tasks-default.webp'),'Tasks default artwork not active at Home');
  assert.equal(await page.locator(`${sel} [data-elara-tab="social"] .elara-nav-art`).count(),1,'Friends navigation artwork missing');
  assert.ok((await page.locator(`${sel} [data-elara-tab="social"] .elara-nav-art`).getAttribute('src'))?.includes('friends-group-icon.webp'),'Friends nav uses wrong artwork');
  assert.equal(await page.locator(`${sel} [data-elara-tab="home"] .elara-nav-art`).first().evaluate(e=>e.complete&&e.naturalWidth>0),true,'Home image load failed');
  if(hasHabits)assert.ok(item.track>=7,'habit progress still hairline');if(hasTasks)assert.ok(item.tasksHit.w>=22,'Task checkbox too small');
  if(width<=700){assert.ok(item.header.h<=60,'mobile Header too tall');assert.equal(await page.locator('.sidebar').evaluate(el=>getComputedStyle(el).display),'none');assert.equal(await page.locator('.bottom-nav [data-elara-tab="social"]').count(),1,'Friends must remain a mobile primary destination');const friends=page.locator('#panel-home [data-ref-friends-open]');assert.equal(await friends.count(),1,'Friends card entry missing');const friendsArt=friends.locator('.ref-friends-open-art');assert.equal(await friendsArt.count(),1,'Friends card artwork missing');assert.ok((await friendsArt.getAttribute('src'))?.includes('friends-tab.webp'),'Friends card button uses wrong artwork');assert.equal(await friendsArt.evaluate(e=>e.complete&&e.naturalWidth>0),true,'Friends card artwork failed');await friends.click();await page.waitForTimeout(80);assert.equal(await page.evaluate(()=>location.hash),'#social','Friends card did not open the real social route');await page.evaluate(()=>window.ElaraOpen('home'));await page.waitForTimeout(80)}
  else{assert.equal(await page.locator('.bottom-nav').isVisible(),false);assert.ok(item.streak.h>=87&&item.streak.h<=113,'desktop streak not expanded');item.sidebarGap=await page.locator('.sidebar [data-elara-tab="home"]').evaluate(el=>{const a=el.querySelector('.elara-nav-art,.elara-icon').getBoundingClientRect(),t=el.querySelector('small').getBoundingClientRect();return Math.round(Math.max(0,Math.max(a.left,t.left)-Math.min(a.right,t.right)))});assert.ok(item.sidebarGap<=16,'sidebar icon and label separated: '+item.sidebarGap)}
  const tasks=page.locator(`${sel} [data-elara-tab="tasks"]`);await tasks.click();assert.equal(await tasks.getAttribute('aria-current'),'page');assert.ok((await tasks.locator('.elara-nav-art').getAttribute('src'))?.includes('nav-tasks-active.webp'),'Tasks active artwork not tied to current route');await page.locator(`${sel} [data-elara-tab="home"]`).click();
  assert.equal(await page.locator('#panel-home .ref-wellness-heading-art').count(),1,'Wellness heading art missing');assert.equal(await page.locator('#panel-home .ref-ranking-heading-art').count(),1,'Ranking trophy art missing');assert.equal(await page.locator('#panel-home .ref-wellness-cell .elara-wellness-art').count(),3,'Water/Sleep/Exercise artwork missing');assert.equal(await page.locator('#panel-home .ref-wellness-cell').nth(3).locator('img').count(),0,'Weight sample artwork must not present fixed 68.0kg as user data');assert.equal(await page.locator('.identity .elara-brand-wordmark, #ref-mobile-brand .elara-brand-wordmark').count()>0,true,'Elara wordmark missing');
  const themeArt=page.locator('#theme-toggle .ref-theme-art');assert.equal(await themeArt.count(),1,'Theme artwork missing');assert.ok((await themeArt.getAttribute('src'))?.includes('icon-mode-night-active.webp'),'Dark mode should use active night artwork');
  const notifArt=page.locator('#ref-header-notifications .ref-notification-art');assert.equal(await notifArt.count(),1,'Notification artwork missing');assert.ok((await notifArt.getAttribute('src'))?.includes('icon-notifications-read.webp'),'Zero unread should use read artwork');
  if(width===390){
    await page.evaluate(()=>{localStorage.setItem('elara_notifications_v1',JSON.stringify([{id:'qa-unread',title:'QA unread',unread:true}]));window.dispatchEvent(new CustomEvent('elara:notifications-changed'))});await page.waitForTimeout(160);assert.ok((await page.locator('#ref-header-notifications .ref-notification-art').getAttribute('src'))?.includes('icon-notifications-unread.webp'),'Unread artwork did not react to real unread record');await page.locator('#ref-header-notifications').click();await page.waitForTimeout(160);assert.ok((await page.locator('#ref-header-notifications .ref-notification-art').getAttribute('src'))?.includes('icon-notifications-read.webp'),'Bell did not return to read after notifications were opened');await page.keyboard.press('Escape');
    await page.locator('#theme-toggle').click();await page.waitForTimeout(140);assert.equal(await page.locator('body').evaluate(b=>b.classList.contains('light')),true,'Theme toggle did not enter light');assert.ok((await page.locator('#theme-toggle .ref-theme-art').getAttribute('src'))?.includes('icon-mode-night-default.webp'),'Light mode should use default night artwork');await page.evaluate(()=>window.ElaraDrawerTest?.setMode?.('amoled'));await page.waitForTimeout(140);assert.equal(await page.locator('body').evaluate(b=>b.classList.contains('amoled')),true,'AMOLED state missing');assert.ok((await page.locator('#theme-toggle .ref-theme-art').getAttribute('src'))?.includes('icon-mode-night-active.webp'),'AMOLED should use active night artwork');await page.evaluate(()=>window.ElaraDrawerTest?.setMode?.('dark'));await page.waitForTimeout(100);
    assert.equal(await page.locator('.ref-task-check[data-ref-task="qa-task"]').count(),1,'test-only Task fixture absent');
    for(const selector of ['.ref-task-check[data-ref-task="qa-task"]','.ref-habit-row[data-ref-habit="qa-habit"] .ref-habit-mark','[data-home-goal-step="qa-step"]']){const ring=await page.locator(selector).first().evaluate(el=>({html:el.innerHTML.trim(),before:getComputedStyle(el,'::before').content,after:getComputedStyle(el,'::after').content}));assert.equal(ring.html,'','unchecked control must be an empty ring: '+selector);assert.ok(ring.before==='none'||ring.before==='""','unchecked control ::before must be empty: '+selector);assert.ok(ring.after==='none'||ring.after==='""','unchecked control ::after must be empty: '+selector)}
    assert.equal(await page.locator('.ref-task-check[data-ref-task="qa-task"] .ref-task-check-art').count(),0,'completed artwork shown on incomplete Task');await page.locator('.ref-task-check[data-ref-task="qa-task"]').click();await page.waitForTimeout(130);
    let state=await page.evaluate(()=>JSON.parse(localStorage.getItem('elara_space_v1')));assert.equal(state.tasks[0].completed,true);assert.ok(state.xp>=10);assert.equal(state.tasks[0].xpAwarded,true);assert.equal(await page.locator('.ref-task-check[data-ref-task="qa-task"] .ref-task-check-art').count(),1,'completed Task did not receive uploaded alpha artwork');
    await page.locator('.bottom-nav [data-elara-tab="tasks"]').click();const canonicalTask=page.locator('#task-list .check-button[data-id="qa-task"][aria-pressed]');assert.equal(await canonicalTask.getAttribute('aria-pressed'),'true','canonical Tasks page does not agree with Home');assert.equal(await canonicalTask.locator('.elara-check-art').count(),1,'Tasks checked artwork missing');const taskFit=await canonicalTask.evaluate(el=>{const p=el.getBoundingClientRect(),c=el.querySelector('img').getBoundingClientRect();return {dw:Math.abs(p.width-c.width),dh:Math.abs(p.height-c.height)}});assert.ok(taskFit.dw<=1&&taskFit.dh<=1,'Tasks checked artwork does not fill the circle');await page.locator('.bottom-nav [data-elara-tab="home"]').click();
    const firstReload=await firstPaintReload(page,'warm');item.warm=firstReload.paint;await page.evaluate(()=>{document.body.classList.add('cloud-ready');document.body.classList.remove('cloud-locked');document.getElementById('cloud-layer')?.setAttribute('hidden','');window.ElaraOpen('home',{history:'replace'});window.ElaraReferenceHome.render()});await page.waitForFunction(()=>document.querySelector('.ref-task-check[data-ref-task="qa-task"]'));
    assert.equal(await page.locator('.ref-task-check[data-ref-task="qa-task"]').getAttribute('aria-pressed'),'true','Task complete failed to persist');assert.equal(await page.locator('.ref-task-check[data-ref-task="qa-task"] .ref-task-check-art').count(),1,'completed artwork did not survive refresh');const xp=await page.evaluate(()=>JSON.parse(localStorage.getItem('elara_space_v1')).xp);await page.locator('.ref-task-check[data-ref-task="qa-task"]').click();state=await page.evaluate(()=>JSON.parse(localStorage.getItem('elara_space_v1')));assert.equal(state.tasks[0].completed,false);assert.equal(state.xp,xp,'uncheck awarded duplicate XP');assert.equal(await page.locator('.ref-task-check[data-ref-task="qa-task"] .ref-task-check-art').count(),0,'completed artwork remained after uncheck');
    await page.locator('.ref-habit-row[data-ref-habit="qa-habit"]').click();await page.waitForTimeout(120);state=await page.evaluate(()=>JSON.parse(localStorage.getItem('elara_space_v1')));assert.equal(state.habits[0].days.includes(new Date().toISOString().slice(0,10)),true,'Home Habit did not use canonical state');assert.equal(await page.locator('.ref-habit-row[data-ref-habit="qa-habit"] .ref-habit-check-art').count(),1,'Home Habit checked artwork missing');await page.evaluate(()=>window.ElaraOpen('habits'));await page.waitForTimeout(100);const canonicalHabit=page.locator('#habit-list .check-button[data-id="qa-habit"]');assert.equal(await canonicalHabit.getAttribute('aria-pressed'),'true','canonical Habits page disagrees with Home');assert.equal(await canonicalHabit.locator('.elara-check-art').count(),1,'Habits checked artwork missing');await page.evaluate(()=>window.ElaraOpen('home'));await page.waitForTimeout(100);
    await page.waitForFunction(()=>document.querySelector('[data-home-goal-step="qa-step"]'));await page.locator('[data-home-goal-step="qa-step"]').click();await page.waitForTimeout(120);state=await page.evaluate(()=>JSON.parse(localStorage.getItem('elara_space_v1')));assert.equal(state.goals[0].steps[0].done,true);assert.equal(await page.locator('[data-home-goal-step="qa-step"] .ref-goal-check-art').count(),1,'Home Goal checked artwork missing');await page.evaluate(()=>window.ElaraOpen('goals'));await page.waitForTimeout(100);const canonicalGoal=page.locator('#goal-list .check-button[data-id="qa-step"]');assert.equal(await canonicalGoal.getAttribute('aria-pressed'),'true','canonical Goals page disagrees with Home');assert.equal(await canonicalGoal.locator('.elara-check-art').count(),1,'Goals checked artwork missing');await page.evaluate(()=>window.ElaraOpen('home'));await page.waitForTimeout(100);
    assert.equal(await page.locator('#ref-header-account .ref-account-equipped-frame').count(),1);await page.evaluate(()=>{localStorage.setItem('elara_visual_wardrobe_guest',JSON.stringify({frame:null}));window.dispatchEvent(new CustomEvent('elara:wardrobe-changed',{detail:{uid:'guest',wardrobe:{frame:null}}}))});await page.waitForTimeout(200);assert.equal(await page.locator('#ref-header-account .ref-account-equipped-frame').count(),0,'stale equipped frame');
    await page.locator('[data-home-quick="task"]').click();await page.locator('#elara-home-quick-title').fill('Created from Home');await page.locator('.elara-home-quick-form [type=submit]').click();state=await page.evaluate(()=>JSON.parse(localStorage.getItem('elara_space_v1')));assert.equal(state.tasks.some(t=>t.text==='Created from Home'),true);
    await page.locator('[data-home-quick="habit"]').click();await page.locator('#elara-home-quick-title').fill('New Home habit');await page.locator('.elara-home-quick-form [type=submit]').click();state=await page.evaluate(()=>JSON.parse(localStorage.getItem('elara_space_v1')));assert.equal(state.habits.some(h=>h.title==='New Home habit'),true);
    await page.locator('[data-home-quick="goal"]').click();await page.locator('#elara-home-quick-title').fill('New Home goal');await page.locator('.elara-home-quick-form [type=submit]').click();state=await page.evaluate(()=>JSON.parse(localStorage.getItem('elara_space_v1')));assert.equal(state.goals.some(g=>g.title==='New Home goal'),true);
    for(const route of ['tasks','goals','books'])for(let n=0;n<3;n++){await page.evaluate(r=>window.ElaraOpen(r),route);await page.waitForTimeout(120);const back=page.locator(`#panel-${route} [data-elara-home-return]`);assert.equal(await back.isVisible(),true,`${route} Back missing on entry ${n}`);await back.click();await page.waitForTimeout(120);assert.equal(await page.evaluate(()=>location.hash),'#home')}
    const goalScroll=page.locator('#panel-home .ref-goals-scroll, #panel-home .ref-goal-scroll').first();if(await goalScroll.count()){const gs=await goalScroll.evaluate(el=>({client:el.clientHeight,scroll:el.scrollHeight,overflow:getComputedStyle(el).overflowY,touch:getComputedStyle(el).touchAction}));assert.ok(gs.overflow==='auto'||gs.overflow==='scroll','Goals internal scroll unavailable');assert.ok(gs.scroll>gs.client,'Goals list must scroll after five items');assert.ok(gs.touch.includes('pan-y'),'Goals touch scroll not enabled');await goalScroll.hover();const before=await goalScroll.evaluate(el=>el.scrollTop);await page.mouse.wheel(0,160);await page.waitForTimeout(60);const after=await goalScroll.evaluate(el=>el.scrollTop);assert.ok(after>before,'Goals mouse wheel did not scroll');await goalScroll.evaluate(el=>el.scrollTop=0);await goalScroll.scrollIntoViewIfNeeded();const box=await goalScroll.boundingBox();assert.ok(box,'Goals scroll box missing');const cdp=await page.context().newCDPSession(page);const x=Math.round(box.x+box.width/2),y=Math.round(box.y+Math.min(box.height-12,box.height*.72));await cdp.send('Input.dispatchTouchEvent',{type:'touchStart',touchPoints:[{x,y}]});await cdp.send('Input.dispatchTouchEvent',{type:'touchMove',touchPoints:[{x,y:y-70}]});await cdp.send('Input.dispatchTouchEvent',{type:'touchEnd',touchPoints:[]});await cdp.detach();await page.waitForTimeout(160);assert.ok(await goalScroll.evaluate(el=>el.scrollTop)>0,'Goals touch swipe did not scroll')}
    for(const file of [...used,'icon-freedom-lotus.webp','icon-wellness-water.webp','icon-exercise-running-shoe.webp','icon-habits-sprout.webp','icon-library-open-book.webp','icon-night-crescent-moon.webp','brand-elara-wordmark.webp','icon-achievement-star.webp','icon-add-button.webp','banner-running-moonlit-mountains.webp']){const res=await page.request.get(`http://127.0.0.1:4173/assets/ui/${file}`);assert.equal(res.status(),200,'installed image unavailable '+file)}
  }
  if(width===1648){item.hard=(await firstPaintReload(page,'hard')).paint;await page.evaluate(()=>{document.body.classList.add('cloud-ready');document.body.classList.remove('cloud-locked');document.getElementById('cloud-layer')?.setAttribute('hidden','');window.ElaraReferenceHome.render()})}
  await page.evaluate(()=>window.ElaraOpen('tasks',{history:'replace'}));await page.waitForTimeout(120);const tasksOverflow=await page.evaluate(()=>Math.max(0,document.documentElement.scrollWidth-innerWidth));assert.ok(tasksOverflow<=2,'Tasks horizontal overflow '+tasksOverflow);await page.screenshot({path:`${out}/tasks-${width}-viewport.png`,fullPage:false});await page.evaluate(()=>window.ElaraOpen('home',{history:'replace'}));await page.waitForTimeout(100);await page.screenshot({path:`${out}/home-${width}-viewport.png`,fullPage:false});await page.screenshot({path:`${out}/home-${width}-full.png`,fullPage:true});assert.deepEqual(item.ui404,[],'UI image 404');assert.deepEqual(item.errors,[],'page errors');
  console.log('HOME-PASS '+JSON.stringify({width,height,overflow:item.overflow,scroll:item.scroll,streak:item.streak.h,track:item.track,firstVisibleMs:item.paint.find(x=>!x.booting)?.ms}));
 }catch(e){item.failure=e.stack||String(e);failures.push(width+': '+e.message);console.error('HOME-FAIL '+width+' '+item.failure)}finally{results.push(item);await page.close()}
}
writeFileSync(out+'/checkpoint-a-browser-evidence.json',JSON.stringify(results,null,2));await browser.close();assert.deepEqual(failures,[]);console.log('PASS: Chromium seven viewports, restored Friends navigation, installed Tasks/Header/Wellness artwork, boot triptychs, and sampled first paint.');

// End-HEAD gate: 2026-09-26 — workflow must produce screenshots for this exact SHA.
