# نقشهٔ راه Elara Space ✦ — به‌روزرسانی ۲۱ سپتامبر ۲۰۲۶

**مرجع اصلی و به‌روز وضعیت محصول:** [رودمپ کامل و چک‌لیست مراحل](docs/PRODUCT-ROADMAP.md)؛ [بک‌لاگ جامع V2](docs/V2-COMPLETE-FEATURE-ROADMAP.md)؛ [داشبورد ادمین و Gemini](docs/ADMIN-GEMINI-ROADMAP.md)؛ [راهنمای محدودیت‌های بتا](README-ELARA-APPROVED-FA.md).

> **نیازمندی‌های جدید و گام بعدیِ مشخص:** [ناوبری موبایل، دکمهٔ بازگشت، تکرار تسک/عادت، پروفایل‌های عمومی/شخصی و Club](docs/MOBILE-NAV-RECURRENCE-PROFILE-CLUB-ROADMAP.md). این سند شامل جزئیات پیاده‌سازی مرحله‌ای، معیار پذیرش و شرط انتشار نهایی روی cPanel است. فقط در رودمپ ثبت شده؛ **خود قابلیت‌ها با ثبت این سند ساخته نشده‌اند**.

## مرجع قطعی طراحی

پنج تصویر تاریکِ تأییدشده که آرن در ۲۰ سپتامبر ۲۰۲۶، ساعت ۱۶:۱۷ UTC در گفتگو دوباره فرستاد: خانهٔ دسکتاپ، دوستان دسکتاپ، رنکینگ دسکتاپ، تنظیمات دسکتاپ و خانهٔ موبایل. طرح‌های بعدی که کاربر رد کرد مرجع نیستند. ویژگی‌های کلیدی: شب کوهستان با بنفش نئونی، نمای عریض رایانه، ریسپانسیو گوشی و منوی پایین، منوی راست دسکتاپ، عادت‌های روز با نام و نوار افقی، بخش‌های مستقل دوستان/رنکینگ، تم‌های رنگی و روشن/تاریک، فارسی/English/Türkçe. برای مطابقت دقیق، خود تصاویر باید در گفتگوی کاریِ جدید در دسترس باشند.

## وضعیت کوتاه

- **کدنویسی مقدماتی شده:** داشبورد و ماژول‌های پایهٔ تسک/عادت/هدف/تمرکز/کتاب/لایتنر، Firebase Auth و ذخیرهٔ حسابی، نام کاربری، درخواست دوستی، فید فعالیت اختیاری، رنکینگ دوستان بر اساس XP فعلی، لقب‌های ده سطح، Light/Dark/System و ترجمهٔ جزئی؛ اسکلت Admin Core با احراز هویت و بررسی نقش در Firestore. نسخهٔ اخیر دکمهٔ نمایش/پنهان‌کردن رمز و پیام دقیق‌تر خطاهای Firebase را در ورود سایت و پنل Admin اضافه کرده است.
- **تأیید نشده یا ناقص:** تست واقعی چند حساب و مرورگرهای مختلف، پایداری Login روی PC گزارش‌شده، امنیت/مقیاس‌بندی اجتماعی، ترجمهٔ یکپارچهٔ تمام UI، آواتار و فریم اختصاصی، همگام‌سازی هم‌زمان چند دستگاه، رنکینگ جهانی/فصلی با امتیاز سروری، چالش‌ها، Clubs، AI واقعی «آزادی»، Shop و سایر بخش‌های V2. وجود UI یا ثبت رودمپ به‌معنی آماده‌بودن نیست.
- **اولویت اجرایی جدید:** ۱) تست و تشخیص دقیق Login در PC/موبایل بدون محدودکردن حساب یا دستگاه؛ ۲) Back/history و طراحی ناوبری موبایل با Dashboard، کتابخانه و زبان مستقل، Missions در داشبورد، More/Settings؛ ۳) تکرار Task/Habit، Folder/Tag inline، Focus قابل تنظیم و رنگ Priority؛ ۴) پروفایل خود/عمومی با Privacy و درخواست دوستی/چالش؛ ۵) Club Core با شرط Level 6 برای کاربران و استثنای امن Owner اصلی. معیارهای دقیق در [سند گام بعدی](docs/MOBILE-NAV-RECURRENCE-PROFILE-CLUB-ROADMAP.md) آمده‌اند.
- **کدنویسی مرحلهٔ Navigation/Mobile Dashboard شروع شد (نیازمند تست کاربر):** پنل مأموریت‌ها با Router جدید هم‌مسیر شده تا صفحهٔ خالی ناشی از اختلاف `rpg-missions`/`panel-missions` برطرف شود؛ Home در نوار پایین ورودی اصلی است، Library و Language مستقل‌اند، More/Drawer مسیر عادت‌ها، هدف‌ها، Focus، Missions، Friends، Ranking، Reports، Freedom، Folder/Tag و Settings را می‌دهد، Back/history/deep-link پایه اضافه شده و Dashboard خلاصهٔ مأموریت‌های واقعی همان حساب را نشان می‌دهد. این موارد **کدنویسی شده‌اند اما هنوز E2E روی گوشی‌های کاربر تأیید نشده‌اند** و «کامل» محسوب نمی‌شوند.
- **ادمین و زیرساخت:** Admin Core اولیه نوشته شده ولی مدیریت حرفه‌ای کاربران، محتوای قابل آپلود، Gemini keys/models و Deploy/Rollback امن هنوز کامل نشده‌اند. هیچ API Key محرمانه‌ای در GitHub، ZIP یا JavaScript قابل دسترسی کاربر ذخیره نشود.

## موارد تازهٔ پیگیری — ورود PC و آیکون نمایش رمز [درخواست‌شده / حل‌نشده]

- **ورود PC:** کاربر تأیید کرده که رایانه از قبل به هات‌اسپات گوشی متصل بوده، تنظیمات Proxy قبلاً بررسی و دستگاه Restart شده، اما Login روی رایانه همچنان پیام عدم ارتباط مرورگر با Firebase Auth را نشان می‌دهد. از کاربر نخواهیم همان آزمایش هات‌اسپات یا حذف گروهی فایل‌های GitHub را تکرار کند. خطا و شواهد قبلی در [AUTH-PC-DIAGNOSTICS.md](docs/AUTH-PC-DIAGNOSTICS.md) ثبت شده‌اند؛ علت دقیق شکست درخواست در این اتصال هنوز با لاگ جدید Network اثبات نشده است. گام عیب‌یابی بدون تغییر کد: از درخواست ناموفق Firebase Auth در DevTools → Network، فقط URL میزبان، نوع خطای شبکه و HTTP status (در صورت وجود) بررسی شود، بدون اشتراک رمز، توکن، API key یا بدنهٔ درخواست؛ سپس Proxy/VPN/افزونه/فایروال/DNS یا پاسخ سرویس بر اساس همان شواهد تفکیک شود. هیچ دسترسی حساب، دستگاه یا Firestore Rule برای دورزدن خطای یک دستگاه محدود نشود.
- **شواهد تازهٔ ورود PC (۲۱ سپتامبر):** روی خود گوشی ورود با VPN و بدون VPN موفق است؛ روی PC متصل به هات‌اسپات، با یا بدون Se7en Pro و در حالت ناشناس و مرورگر دیگر ورود ناموفق است؛ Chrome نیز به‌روزرسانی شده و مشکل باقی مانده. در DevTools درخواست preflight واقعی `OPTIONS` به `identitytoolkit.googleapis.com/v1/accounts:signInWithPassword` با 403 و CORS متوقف شده است؛ پاسخ HTML گزارش شده و محتوای آن در DevTools نمایش داده نشده. اجرای `curl` در CMD **بدون VPN رایانه** برای مسیر `/` روی IPv4 و IPv6 و برای `OPTIONS /v1/accounts:signInWithPassword` بدون پارامتر API key نیز 403 HTML عمومی گوگل داده است؛ این آزمایش‌های بدون کلید و بدون بدنه، معادل درخواست واقعی SDK نیستند و به‌تنهایی مسدودشدن PC توسط Firebase یا خطای پیکربندی پروژه را ثابت نمی‌کنند. قبل از هر نتیجه‌گیری، پاسخ و هدرهای **همان preflight واقعی** روی دستگاه/مسیر سالم و ناموفق را با اطلاعات محرمانهٔ پوشانده‌شده مقایسه و در صورت نیاز تنظیمات محدودیت API key و مسیر شبکهٔ ویندوز را جداگانه بررسی کنیم؛ از تکرار آزمایش‌های انجام‌شده یا تغییر دادن Firestore Rules برای رفع این خطا خودداری شود. **وضعیت: علت قطعی نامشخص، حل و تست نشده.**
- **دکمهٔ نمایش/پنهان‌کردن رمز در Login/Registration و Admin:** کاربر آیکون فعلی چشمِ ایموجی و به‌خصوص ایموجی میمونِ حالت نمایش رمز را نمی‌پسندد. `cloud.js` فعلی به‌صورت مستقیم `👁` و `🙈` را در `password-toggle` قرار می‌دهد؛ این یک انتخاب ظاهری مستقل از خطای اتصال Firebase است. پس از درخواست صریح کدنویسی، هر دو حالت با آیکون استاندارد و غیرایموجی Eye / Eye-off هماهنگ با طراحی Elara جایگزین شوند؛ دکمهٔ قابل‌دسترسی با برچسب متنی، نمایش درست در RTL/LTR، وضعیت pressed و پشتیبانی صفحه‌کلید حفظ شود؛ پنل Admin نیز بررسی و یکسان‌سازی شود. هیچ تغییری در منطق Auth یا داده‌های کاربران لازم نیست. **وضعیت: فقط ثبت نیازمندی؛ کد تغییر نکرده و تست انجام نشده است.**

## گزارش تازهٔ موبایل و نیازمندی اجتماعی — ۲۱ سپتامبر ۲۰۲۶ [درخواست‌شده / تست‌نشده]

- **نمایش مأموریت‌ها روی همهٔ گوشی‌ها و حساب‌ها:** کاربر گزارش کرده مأموریت‌های امروز روی گوشی خودش دیده می‌شود ولی روی گوشی دوستش محتوای مأموریت‌ها نمایش داده نمی‌شود (اسکرین‌شاتِ گوشی دوست: تب «مأموریت‌ها» با فضای خالی و نوار ناوبری پایین؛ اسکرین‌شاتِ گوشی خود: کارت‌های مأموریت). این گزارش، با نیازمندی قبلیِ دسترسی واضح به Missions از Dashboard و ناوبری موبایل ادغام می‌شود و به معنی نبودن داده یا مشکل قطعی CSS نیست. بررسی جداگانهٔ رندر پنل، وضعیت بارگذاری/خطا/خالی، تفاوت نسخهٔ کش‌شده یا فایل‌های JS/CSS، viewport، account state، locale، شکست اجرای JavaScript و داده‌های واقعی مأموریت برای **هر حساب**؛ هیچ مأموریت یا امتیاز نمونه‌ای برای پوشاندن مشکل به حساب واقعی اضافه نشود. معیار پذیرش: با دو حساب مستقل روی دو گوشی و عرض‌های کوچک، مأموریت‌های واقعی و وضعیت پیشرفت هر حساب در Dashboard و صفحهٔ مأموریت‌ها قابل مشاهده باشد؛ در صورت نداشتن مأموریت، Empty State روشن نمایش داده شود و پس از Refresh/تعویض حساب نیز درست بماند. **وضعیت: گزارش باگ کاربر؛ علت معلوم نیست، رفع و E2E انجام نشده است.**
- **لقب دادن به دوستان — تصمیم نهایی و اصلاح نیازمندی قبلی:** کاربر بتواند برای دوست پذیرفته‌شده لقب دلخواه تعیین/ویرایش/حذف کند. **نام اصلی/نام نمایشی و Username دوست همیشه حفظ و نمایش داده شود؛ لقب فقط کنار نام اصلی یا در جای جداگانه و خوانا در همان ردیف/کارت نشان داده شود، نه به‌جای نام.** حالت «لقب خصوصی که فقط تعیین‌کننده می‌بیند» مورد نظر کاربر نیست؛ پیشنهاد قبلیِ لقب خصوصی در این سند لغو شده است. پیش از پیاده‌سازی، دامنهٔ نمایش لقب برای دوست و دیگران، رضایت و مجوز ویرایش آن را با حفظ حریم خصوصی روشن کنیم؛ هیچ کاربر نتواند نام یا پروفایل اصلی دیگری را با لقب خود تغییر دهد. لقب دوستان از Title/لقب سطح، Rank و Username جداست؛ همپوشانی آن با Relationship Nickname در رودمپ V2 نباید نیازمندی نهایی این بخش را تغییر دهد. معیار پذیرش: روی گوشی و دسکتاپ نام اصلی و لقب هم‌زمان قابل رؤیت باشند؛ تغییر/حذف لقب نام اصلی را عوض نکند. **وضعیت: صرفاً ثبت نیازمندی؛ ساخته و تست نشده است.**
- **اعلان کوتاه تکمیل مأموریت و مأموریت بعدی:** با تکمیل واقعی هر مأموریت، یک Toast/اعلان کوچک و غیرمزاحم در موبایل و دسکتاپ نشان داده شود: «مأموریت [نام] تکمیل شد» و نام/عنوان **مأموریت بعدی واقعیِ قابل انجام همان حساب** را نیز نشان دهد. در صورت نبود مأموریت بعدی، پیام کوتاهِ مناسب به‌جای ساختن مأموریت نمونه نمایش داده شود. اعلان دکمهٔ ضربدرِ قابل لمس برای بستن فوری داشته باشد، خودکار پس از حدود **۲ تا ۳ ثانیه** ناپدید شود، صفحه را نپوشاند و از نظر چیدمان RTL/LTR و صفحهٔ کوچک مناسب باشد. یک **صدای کوتاه هنگام تکمیل واقعی** پخش شود، با رعایت Mute/تنظیمات صدای کاربر، محدودیت autoplay مرورگر و ترجیح Reduce Motion؛ اعلان بدون صدا نیز پیام را منتقل کند. برای هر تکمیل واقعی فقط یک اعلان/صدا صادر شود؛ Refresh، تکرار رویداد و همگام‌سازی نباید اعلان یا XP تکراری بسازند. این مورد مکمل نیازمندی‌های پیشین Missions و Notification است و اعلان دائمی/بزرگ یا ارسال اعلان به دوستان نیست. معیار پذیرش: تکمیل مأموریت با دو حساب مستقل روی گوشی و دسکتاپ، نمایش نام مأموریت تمام‌شده و بعدی، بستن با ضربدر، محوشدن خودکار ۲–۳ ثانیه‌ای، صدای کوتاه با رعایت Mute و عدم دوباره‌اعلان پس از Refresh. **وضعیت: فقط رودمپ؛ کد/تست انجام نشده است.**


## پیش‌نمایش توسعه از GitHub [زیرساخت اضافه شد / نیازمند اجرای موفق Pages]

- برای مشاهدهٔ نسخهٔ شاخهٔ `main` بدون دست‌زدن به ریپوی انتشار محدثه، Workflow جداگانهٔ GitHub Pages اضافه شده است. این Workflow فقط فایل‌های Runtime سایت (HTML/CSS/JS، assets و admin) را داخل Artifact پیش‌نمایش قرار می‌دهد و اسناد خصوصی رودمپ، READMEها و Firestore Rules را منتشر نمی‌کند.
- آدرس مورد انتظار پیش‌نمایش توسعه: `https://arenparsi.github.io/elaraspace/`. این آدرس **محیط تست توسعه** است، نه انتشار نهایی و نه جایگزین تصمیم انتقال نهایی به cPanel. فعال‌بودن/موفق‌شدن GitHub Pages باید از نتیجهٔ Workflow تأیید شود.


## اجرای فاز Tasks/Habits/Focus — ۲۱ سپتامبر ۲۰۲۶ [کدنویسی‌شده / در انتظار تست کاربر]

- Task و Habit اکنون قرارداد دادهٔ recurrence مستقل (weekdays/startDate/endDate/timezone) دارند؛ تکمیل‌های هر نوبت، XP دریافت‌شده، Skip و Override تاریخ‌دار جدا از خود سری نگه‌داری می‌شوند تا رکوردهای بی‌نهایت ساخته نشوند و دادهٔ قبلی حذف نشود.
- فرم Task امکان تکرار هفتگی، پایان/بدون پایان، ساخت Folder/Tag در همان فرم و فیلتر Priority دارد؛ ویرایش «فقط امروز / از امروز به بعد» و حذف «نوبت امروز / کل سری» در پایه کدنویسی شده است. Habit نیز زمان‌بندی هفتگی و همان مدل نوبت مستقل را دارد.
- Focus دارای مدت‌های آماده و مدت دلخواه ۱–۱۸۰ دقیقه، Tag، Pause/Resume/Cancel، Session فعال قابل بازیابی بعد از Refresh و تاریخچهٔ جلسات تکمیل‌شده است. XP هر Session با شناسهٔ یکتا فقط یک‌بار ثبت می‌شود.
- Priority با متن و رنگ واحد P1 قرمز، P2 زرد، P3 آبی و P4 خاکستری در لیست/فیلتر تسک نمایش داده می‌شود؛ رنگ تنها نشانه نیست.
- این فاز **کدنویسی شده ولی تست کاربر نشده است**؛ timezone، پایان سری، Refresh، چند حساب، موبایل، Theme و عدم XP تکراری باید قبل از «کامل» شدن بررسی شوند.

## نیازمندی‌های افزودهٔ ۱۳:۲۰ — Theme/Message/Admin/Mobile [ثبت‌شده]

- Theme Engine باید دو محور مستقل داشته باشد: Mode/Accent و **Style Family**. خانواده‌های اولیه: Default/Elara فعلی، Minimal ظریف، Dark/Rugged خشن، Anime/Fantasy؛ تغییر Style باید گرافیک، Card، typography، background، icon و motion را محسوس عوض کند. Pure Black/AMOLED و بازگشت Default هم اضافه شود.
- Mobile Dashboard در فاز UI به طراحی کاملاً Box/Card-based بازسازی شود؛ طرح فعلی موبایل نهایی نیست. Help نیز پایین More/Settings/Folder-Tag در دسترس باشد.
- Message Center مستقل با تاریخچهٔ Mission completion، پیام‌های System/Admin، Challenge request/result و Quick Chat؛ Toast مأموریت ۲–۳ ثانیه‌ای و صدا در کنار History دائمی. Challenge request حدود ۳۰ ثانیه اعتبار سروری داشته باشد.
- Admin Dashboard: Site Preview با نوار کنترل جدا، Visitor Analytics واقعی و privacy-preserving، و مدیریت System Messages با Audit/Backend معتبر.
- لقب دوست کنار نام اصلی نمایش یابد و نام اصلی حفظ شود؛ nickname خصوصیِ فقط برای خود کاربر جزو طرح نهایی نیست.
- این موارد **به رودمپ اضافه شده‌اند، نه اینکه با ثبت سند ساخته شده باشند**؛ در فازهای Theme/UI، Message/Challenge و Admin جداگانه کدنویسی و تست می‌شوند.


## اجرای فاز Profile/Friends — ۲۱ سپتامبر ۲۰۲۶ [کدنویسی پایه / در انتظار تست کاربر]

- Profile مشترک Self/Public، بازشدن از Friends/Leaderboard، ویرایش Name/Bio/Username و Public Profile ساخته شد. Username change با Transaction و Username claim جدید/حذف claim قبلی طراحی شده و Rules متناظر در Git ثبت شده‌اند.
- Friend lifecycle پایه تکمیل‌تر شد: مشاهده Profile، ارسال/لغو Request، قبول/رد و Remove Friend. Challenge دکمهٔ غیرفعال دارد تا قبل از منطق معتبر سمت سرور قابلیت جعلی نمایش داده نشود.
- Profile فقط metadata نمایشی را نگه می‌دارد؛ Task/Habit/Goal/private payload وارد سند عمومی Profile نشده‌اند. Rules برای Profile محدودشده، Friends و Request participant مسیر خواندن کنترل‌شده دارند.
- **نیازمند تست/Publish:** Firestore Rules باید در Firebase واقعی Publish و با دو UID تست شوند؛ Username race، Privacy، Remove/Resend Request، Refresh، Mobile و خطاهای permission-denied قبل از «کامل» شدن باید بررسی شوند.


## اصلاح Tasks / Missions / Private Profile Drawer — ۲۱ سپتامبر ۲۰۲۶ [کدنویسی‌شده / نیازمند تست کاربر]

- Task دارای **توضیح کوتاه (تا ۲۸۰ نویسه)** و **توضیحات کامل (تا ۴۰۰۰ نویسه)** شد. کلیک روی خود تسک باید مودال جزئیات وسط صفحه را باز کند و Title، Short/Long Description، Date، Time، Priority، Folder، Tag و Recurrence را همان‌جا قابل ویرایش کند.
- ساخت Folder/Tag از فرم Task از Popup بومی مرورگر به **Elara Dialog** منتقل شد و Draft فرم (عنوان، توضیحات، تاریخ، ساعت، اولویت، Folder/Tag و Recurrence) باید قبل/بعد از ساخت مورد جدید حفظ شود؛ ریست شدن فیلدهای فرم باگ محسوب می‌شود.
- Prompt/Confirmهای جریان Task/Habit/Focus و بخش‌های اصلی برنامه به Dialog مشترک، وسط صفحه و Theme-aware منتقل شده‌اند. استفاده از Alert/Prompt/Confirm بومی مرورگر در UI نهایی ممنوع است مگر fallback اضطراری.
- Missionها Reward قابل مشاهده دارند و Reward XP روزانه با Claim تاریخ‌دار فقط یک‌بار ثبت می‌شود؛ نمایش Reward و حالت «دریافت شد» در کارت مأموریت الزامی است.
- **سه‌نقطهٔ مستقل گوشهٔ صفحه** جای «انبار دکمه‌ها در More» را می‌گیرد. روی Desktop Drawer کناری و روی Mobile Drawer/Bottom Sheet نیم‌صفحه باز می‌شود. **بخش‌های اصلی مثل Home/Tasks/Habits/Goals/Focus/Library/Language/Missions/Friends/Ranking داخل Drawer قایم نشوند**؛ موبایل یک نوار اصلی افقی/اسکرول‌پذیر با الهام از Sidebar دسکتاپ دارد و Drawer فقط Account/Privacy/Folders/Notifications/Appearance/Help/Calendar/Logout است.
- بالای Drawer پروفایل خصوصی کاربر (Avatar placeholder/عکس آینده، Name، Bio، Level/Title/XP و Medal summary) و Dark/Light quick toggle نمایش داده می‌شود. زیر آن به‌ترتیب Account، Privacy/Security (Password reset)، Folders & Tags، Notifications/Messages، Appearance/Themes، Help، Calendar و Logout قرار می‌گیرند.
- بخش Folders & Tags داخل Drawer پوشه‌ها را Expand می‌کند، تسک‌های هر پوشه را نشان می‌دهد و امکان ساخت Task جدید **همان‌جا داخل پوشه** دارد.
- Public Profile دیگر Route افقی مستقل نیست و باید به‌صورت **مودال مستطیلی وسط صفحه** باز شود. Private/Self Profile از Drawer مدیریت می‌شود.
- Refresh روی hash قدیمی Profile نباید صفحهٔ خالی نشان دهد؛ route قدیمی profile به Home fallback می‌کند.
- Save Profile برای Name/Bio با Rules قدیمی هم مسیر سازگار دارد؛ Username و Profile Privacy در صورت Publish نبودن Rules جدید نباید کل Save را fail کنند و پیام محدودیت همان قسمت نمایش داده شود. برای فعال‌شدن واقعی Username/Privacy جدید، firestore.rules باید در Firebase Publish شود.
- این بسته هنوز تست واقعی کاربر روی Mobile/Desktop، دو حساب، Firebase Rules منتشرشده، Recurrence details و Mission XP duplicate را پاس نکرده است.


## سیاست انتشار و توسعه

تا زمانی که قابلیت‌های مورد توافق ساخته و روی موبایل/دسکتاپ، حساب‌های جدا، Firestore Rules و فرایندهای اصلی تست نشده‌اند، پروژه به **cPanel و دامنهٔ خریداری‌شده منتقل نشود**؛ محیط توسعه/آزمایشی فعلی جدا بماند. بعد از تکمیل، با هماهنگی دارندهٔ دسترسی cPanel، Backup، DNS، SSL و Deploy/rollback انجام شود.

هنگامی که نیازمندی جدیدی در گفت‌وگو مطرح می‌شود و ثبت/تغییر GitHub درخواست شده است، تغییر واقعی را در `ArenParsi/elaraspace` Commit و لینک فایل/Commit را در همان پاسخ ارائه کنید. این دستور به معنی ساخت خودکار هر ایده یا Commit صوری برای هر پیام نیست: وضعیت‌های «درخواست‌شده»، «کدنویسی‌شده»، «تست‌شده» و «آمادهٔ انتشار» باید جدا بمانند. در صورت نداشتن دسترسی نوشتن Git، عدم موفقیت صریحاً اطلاع داده شود.

`ArenParsi/elaraspace` ریپوی خصوصی توسعه و `Mohadesehjohari/elaraspace` ریپوی انتشار فعلی‌اند و **خودکار همگام نمی‌شوند**. تغییر فایل Rules در GitHub به‌تنهایی قواعد را در Firebase فعال نمی‌کند. قبل از انتشار/مهاجرت از داده‌ها و تنظیمات Backup گرفته شود.
