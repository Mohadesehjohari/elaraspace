# انتقال یک‌بارهٔ Elara Space به مخزن جدید

مخزن توسعه و انتشار از این پس `Mohadesehjohari/elaraspace` است. مخزن قدیمی `ArenParsi/elaraspace` خصوصی است؛ صرف اتصال آن به ChatGPT به معنی در دسترس بودنش برای GitHub Actions مخزن جدید نیست.

## راه‌اندازی انتقال

1. در حساب `ArenParsi` یک GitHub fine-grained personal access token بسازید که **فقط** به مخزن `ArenParsi/elaraspace` دسترسی `Contents: Read-only` داشته باشد. رمز را در چت، کامیت یا فایل عمومی قرار ندهید.
2. در حساب `Mohadesehjohari`، در `elaraspace → Settings → Secrets and variables → Actions` یک Repository secret به نام `ELARA_SOURCE_READ_TOKEN` بسازید و مقدار توکن مرحلهٔ قبل را آنجا وارد کنید.
3. از تب `Actions` مخزن جدید، گردش‌کار `Import Elara from private source (one time)` را انتخاب و `Run workflow` را برای `main` اجرا کنید. این گردش‌کار نسخهٔ مشخص‌شده در فایل workflow از مخزن قدیمی را می‌خواند، فقط فایل‌های مجاز را کپی می‌کند، تصاویر موجود مخزن جدید را حفظ می‌کند، ساختار ابتدایی و سینتکس را تست می‌کند و سپس تغییرات را در `main` مخزن **جدید** ثبت می‌کند.
4. در تب `Actions` اجرای `Validate Elara site` و انتشار GitHub Pages را بررسی کنید، سپس آدرس `https://mohadesehjohari.github.io/elaraspace/` را در مرورگر آزمایش کنید. اگر Pages بر اساس شاخهٔ `main` و پوشهٔ `/ (root)` تنظیم نشده، در `Settings → Pages` همان را انتخاب کنید.
5. پس از انتقال موفق، چون این عملیات یک‌باره است، secret و توکن دسترسی منبع را حذف یا منقضی کنید. توسعه و تغییرات بعدی را فقط روی مخزن جدید انجام دهید.

## نکات امنیتی و کارکرد

مخزن جدید **عمومی** است؛ فایل‌ها و مستندات مجاز برای کپی قبل از انتشار باید از نظر دادهٔ شخصی و اسرار بررسی شوند. workflow چند الگوی رایج کلید محرمانه را مسدود می‌کند اما جایگزین بازبینی امنیتی کامل نیست. محتوای `.env`، تنظیمات `.github` مخزن قدیمی و فایل‌های پنهان به‌صورت خودکار منتقل نمی‌شوند. تصویرهای آواتار/فریم موجود در مخزن مقصد حذف یا جایگزین نمی‌شوند مگر فایل هم‌نامی در منبع مجاز وجود داشته باشد.

فایل `firestore.rules` صرفاً در GitHub نسخه‌بندی می‌شود و **خودکار روی Firebase اعمال نمی‌شود**. تأیید مجاز بودن دامنه `mohadesehjohari.github.io` در Firebase Authentication و آزمون واقعی ثبت‌نام، ورود و Firestore بعد از انتشار لازم است. موفقیت GitHub Pages به‌تنهایی صحت این قابلیت‌ها را ثابت نمی‌کند.
